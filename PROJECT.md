# 日用品庫存管理系統

## 專案目標

建立一個日用品庫存管理網站。先完成電腦網頁版全部功能，再製作手機網頁版。資料儲存在 Google Sheet，後端使用 Google Apps Script Web App API。

系統用於管理台北、台中兩個區域的日用品庫存，支援日常增減、定期盤點、安全庫存提醒與採買清單。

## 工作守則

- 當使用者詢問「上次做到哪」、「目前進度」或同義語句時，讀取本文件並依「專案階段」的狀態與最近工作紀錄回報專案進度。
- 當使用者說「今天做到這裡」、「我要休息了」或同義語句時，將當天已實作、驗證或完成的內容更新到本文件：更新對應 phase 的狀態，並在「工作紀錄」新增日期與摘要。
- 當使用者明確說出「phase x 完成」或同義語句時，才能將該 phase 的細節更新到本文件；否則不主動變更 `PROJECT.md` 的 phase 細節與狀態。
- 尚未驗證完成的工作不可標示為「已完成」；可標示為「進行中」，並記錄下一步。
- 每完成一個 phase，提供一個可操作的範例或明確的驗證步驟，供使用者確認該 phase 功能正常後再進入下一階段。

## 技術方向

- 前端：先以電腦網頁版為主要設計與驗收目標；全部功能完成後，再進行手機版響應式介面製作與驗收。
- 後端：Google Apps Script，以 `doPost(e)` 按 `action` 分流 API。
- 資料庫：Google Sheet。
- 登入：PIN 登入，登入成功後發放短效 token；token 預計由 `CacheService` 保存 6 小時。
- 發佈：使用 GitHub Pages 發佈靜態前端，供私人使用。
- 圖示：介面統一使用 Google Material Icons。

## 資料表

### `SafetyStock`

| 商品 | 安全庫存 | 管理狀態 |
| --- | ---: | --- |

- 管理狀態：`列管`、`暫停`、`封存`

### `Quantity`

| 區域 | 類別 | 商品 | 庫存 | 單位 | 購買途徑 | 備註 |
| --- | --- | --- | ---: | --- | --- | --- |

- 區域：`台中`、`台北`

### `History`

| 時間 | 區域 | 商品 | 動作 | 異動量 | 舊庫存 | 新庫存 |
| --- | --- | --- | ---: | ---: | ---: | ---: |

- 動作：`increase`、`decrease`、`audit`

## 已知 API

| Action | 用途 |
| --- | --- |
| `login` | 用 PIN 登入並取得 token |
| `getInventory` | 取得庫存資料 |
| `updateInventory` | 增加或減少指定商品庫存 |
| `auditInventory` | 儲存盤點後的實際庫存 |
| `getShoppingList` | 取得低於安全庫存的採買清單 |
| `getSafetyStock` | 取得安全庫存設定 |
| `updateSafetyStock` | 更新安全庫存與管理狀態 |

## 頁面與功能

### 登入頁

- 顯示「日用品盤點系統」。
- 輸入 PIN 後登入。
- 提供免登入的購物清單入口。

### 首頁

- 顯示目前區域（台北／台中）。
- 顯示待補貨品項數。
- 提供「庫存模式」、「盤點模式」、「購買清單」與「設定」入口。

### 庫存模式

- 選擇區域、搜尋商品、依類別篩選與選擇商品。
- 顯示目前庫存與單位。
- 輸入異動數量後，執行增加或減少。
- 每次異動需寫入 `History`。

### 盤點模式

- 選擇區域與類別。
- 顯示商品、目前庫存與實際盤點輸入欄位。
- 儲存後以實際庫存覆寫，並留下 `audit` 紀錄。

### 購買清單

- 不需要登入或 token 驗證即可開啟。
- 依區域呈現需要補貨的列管商品。
- 顯示目前庫存／安全庫存，並提示另一區域庫存。
- 提供勾選已購買的介面；勾選僅為前端短暫狀態，不寫回 Google Sheet，也不跨裝置同步。

### 設定頁

- 維護商品安全庫存。
- 切換商品管理狀態（列管／暫停／封存）。
- 設定網站主題色；主題色套用於按鈕、選取狀態與重點資訊。
- 主題色初期可儲存在瀏覽器，若需要跨裝置同步，再擴充後端設定儲存。

## 專案階段

每個 phase 聚焦交付一項可驗收功能；完成時應更新本表的狀態與實際完成日期。

| Phase | 功能 | 驗收重點 | 狀態 |
| --- | --- | --- | --- |
| 0 | 專案基礎與共用版型 | 建立前端專案、電腦版共用版型、API 呼叫與錯誤處理基礎 | ✅ 已完成 |
| 1 | PIN 登入 | 可輸入 PIN、呼叫 `login`、保存 token、登入失敗顯示訊息 | ✅ 已完成 |
| 2 | 首頁儀表板與區域切換 | 可切換台北／台中，首頁顯示目前區域；待補貨數於 Phase 6 串接購買清單資料後顯示 | ✅ 已完成 |
| 3 | 庫存商品選擇 | 可取得庫存，並以區域、類別與商品搜尋／選擇，顯示目前庫存與異動欄位 | ✅ 已完成 |
| 4 | 庫存增減 | 可輸入異動量並執行增加／減少，畫面更新且後端產生歷程 | ✅ 已完成 |
| 5 | 盤點模式 | 可依區域／類別批次輸入實際庫存並儲存盤點 | ✅ 已完成 |
| 6 | 購買清單 | 不需 token 即可依區域顯示低於安全庫存的列管商品 | ✅ 已完成 |
| 7 | 購買項目勾選 | 可在清單勾選已購買項目；勾選僅在前端暫存，不跨裝置同步 | ✅ 已完成 |
| 8 | 安全庫存設定 | 可讀取、編輯安全庫存與管理狀態 | ✅ 已完成 |
| 9 | 主題色設定 | 可選擇主題色、即時套用，並保存使用者選擇 | ✅ 已完成 |
| 10 | 重構 | 將每個頁面都變成獨立的 HTML | ✅ 已完成 |
| 11 | 手機版介面 | 在所有桌面功能完成後，製作並驗證手機版的響應式版型、導覽與操作流程 | ✅ 已完成（2026-09-11） |
| 12 | 檢查Bug | 需要改善地方 | 🔄 進行中 |
| 13 | 購買清單顯示其他區域 | 有其他區域庫存時，在卡片右上角以標籤顯示；沒有則隱藏 | ✅ 已完成 |
| 14 | 發布 | 使用 Github page 發布 | ❌ 未開始 |

## Phase 0 實作細節（已完成）

### 交付檔案

| 檔案 | 用途 |
| --- | --- |
| `index.html` | 純桌面版共用頁面骨架、頂部導覽、區域選擇器、設定按鈕與 Toast 容器。 |
| `styles.css` | 桌面版的色彩變數、共用卡片、頂部導覽、頁面版面與錯誤提示樣式。 |
| `app.js` | Hash 路由、首頁功能入口、區域選擇暫存與全域錯誤提示。 |
| `api.js` | Google Apps Script `POST` client、API 回應解析及 `ApiError` 錯誤類別。 |

### 已完成行為

- 以電腦網頁版為唯一實作與驗收目標；頁面最小寬度為 960px，沒有手機版導覽與手機專屬操作。
- 頂部提供「首頁、庫存模式、盤點模式、購買清單」的 Hash 路由導覽，以及設定按鈕。
- 首頁顯示三個功能入口；其餘功能頁目前為後續 phase 的預留頁面。
- 可切換台北／台中；選擇會以瀏覽器 `localStorage` 的 `inventory-region` 保存。
- 已建立統一 Toast 錯誤提示與 API 錯誤類別，供後續 API 串接共用。
- `api.js` 以 `POST` 傳送 JSON；Google Apps Script 網址集中設定於 `API_ENDPOINT`，目前保持空白，等待後端部署後填入。
- 共用圖示使用 Google Material Icons：`home`、`inventory_2`、`fact_check`、`shopping_cart`、`settings`。

### 尚未納入 Phase 0

- PIN 登入、token 保存與實際 API 呼叫。
- Google Sheet／Apps Script endpoint 設定。
- 真實庫存、盤點、安全庫存、購買清單與主題色功能。
- 手機版響應式設計與驗收（Phase 11）。

## Phase 1 實作細節（已完成）

### 登入與 session

- 未登入時，首頁、庫存模式、盤點模式與設定頁會自動導向 `#login`。
- 登入頁提供 PIN 輸入欄、登入按鈕，以及同尺寸的「直接查看購買清單」次要按鈕。
- 提交登入時，前端以 `postApi("login", { pin })` 呼叫 Apps Script。
- 登入成功後，將後端回傳的 `token` 與 `name` 寫入瀏覽器 `sessionStorage`：
  - `inventory-token`
  - `inventory-user`
- `sessionStorage` 會在瀏覽器／分頁工作階段結束後清除；不保存 PIN。
- 登入後跳轉至首頁，首頁標題會顯示「你好，{使用者名稱}」，例如「你好，Winnie」。

### 權限與導覽

- 已登入時顯示頂部主導覽、區域選擇器、設定按鈕與登出按鈕。
- 登出會移除上述兩個 session 值，並返回登入頁。
- 購買清單（`#shopping`）屬於公開頁面，不需 token 即可開啟。
- 未登入時，頂部的管理導覽、區域選擇與設定功能會隱藏。

### 錯誤處理與部署設定

- 登入按鈕在請求期間顯示「登入中…」並暫時停用，避免重複送出。
- PIN 錯誤、網路失敗、無法辨識的回應與未設定 API URL，皆會以 Toast 顯示錯誤訊息。
- 上線前必須在 `api.js` 的 `API_ENDPOINT` 填入已部署 Apps Script Web App、且以 `/exec` 結尾的 URL。
- 前端已依既有後端的 JSON POST 格式串接；Apps Script 網址尚未填入時，系統會明確提示尚未設定。

## Phase 2 實作細節（已完成）

### 首頁儀表板

- 首頁不再重複顯示「庫存模式、盤點模式、購買清單」入口卡；這些功能統一由頂部導覽列開啟。
- 登入後首頁標題顯示「你好，{使用者名稱}」，例如「你好，Winnie」。
- 首頁副標題會顯示目前區域的概況，例如「台北的日用品庫存概況」。
- 儀表板提供「目前區域」資訊卡，明確顯示台北或台中，並提示可從右上角切換。
- 首頁保留「待補貨」資訊卡；目前以 `—` 顯示，避免在尚未定義購買清單回傳格式前填入假資料。
- 首頁以說明文字引導使用者透過頂部導覽進行庫存異動、盤點或查看購買清單。

### 區域切換

- 右上角提供台北／台中選擇器。
- 區域選擇儲存於瀏覽器 `localStorage` 的 `inventory-region`。
- 切換後會立即重新渲染首頁資訊；重新整理頁面後仍保留最後選擇的區域。

### 範圍調整

- 真實待補貨數量與待補貨商品清單，統一延至 Phase 6 以 `getShoppingList` 串接，避免與購買清單功能重複實作。

## Phase 3 實作細節（已完成）

### 庫存模式操作流程

- 庫存模式不是庫存總表；目標是快速定位單一商品並直接進行庫存異動。
- 進入頁面後，以目前選擇的區域讀取庫存資料。
- 可用商品名稱／備註搜尋，並以類別篩選；商品下拉選單會只顯示符合目前條件的品項。
- 進入頁面、切換區域、變更搜尋或類別後，會自動選取第一個符合條件的商品，不需要先額外選擇才看得到內容。
- 商品異動卡與上方搜尋／篩選列同寬，顯示類別、商品、備註、目前庫存、單位、異動數量欄位及增加／減少按鈕。
- 增加／減少按鈕在本 phase 僅作為操作介面，保持停用；Phase 4 會接上真正的庫存異動。

### `getInventory` 串接格式

- 前端請求：`{ action: "getInventory", token, area: "台北" }`。
- 前端可直接讀取後端 `success: true` 回傳的 `data` 陣列。
- 對應後端中文欄位：`區域`、`商品`、`類別`、`庫存`、`單位`、`備註`。
- 後端目前只回傳管理狀態為「列管」的商品；前端依回傳資料進行搜尋與類別篩選。
- 未設定 Apps Script URL、載入中、查無資料與 API 錯誤都有明確畫面與重新整理操作。

## Phase 4 實作細節（已完成）

### 已完成前端流程

- 庫存模式的增加／減少按鈕已接上 `updateInventory`，會依目前選擇的區域與商品送出異動請求。
- 前端送出的 payload 目前包含：`token`、`area`、`product`、`category`、`quantity`、`changeType`。
- 送出期間會暫時停用異動欄位與按鈕，並將按鈕文字切換為「增加中…」或「減少中…」，避免重複送出。
- 異動成功後，畫面會先更新該商品的目前庫存，再強制重新讀取目前區域的庫存資料，確保前端顯示與後端一致。
- 成功與失敗都會以 Toast 顯示結果；成功訊息使用獨立樣式。

### 前端驗證與保護

- 異動數量必須是大於 0 的整數，否則不送出。
- 若目前庫存可解析為數字，且執行減少時輸入值大於目前庫存，前端會先阻擋並提示錯誤。

### 驗證結果

- 前端已對齊 `body.changeType`，並會以 `increase`／`decrease` 傳入後端。
- 異動成功後，前端會重設異動數量為 `1`，並重新讀取目前區域的庫存資料。
- 切換區域、搜尋條件、類別或商品時，異動數量會回到預設值 `1`。
- 減少異動數量大於目前庫存時，前端會先阻擋並顯示錯誤訊息。

## 已確認範圍

- 商品、類別、單位與區域的基本資料不納入設定頁管理。
- `History` 不額外記錄操作人員名稱與紀錄備註。

## Phase 5 實作細節（已完成）

### 已完成前端流程

- 盤點模式已接上 `getInventory`，會依目前區域讀取庫存資料並以類別篩選。
- 可在盤點清單中逐筆輸入實際盤點數量，前端會暫存在頁面狀態中。
- 已加入「重新整理」與「儲存盤點」按鈕，儲存時會呼叫 `auditInventory`。
- 盤點送出時會帶入 `token`、`area` 與 `items`，其中 `items` 會以 JSON 字串傳送，每筆只包含 `product` 與 `quantity`。

### 驗證結果

- 前端已對齊 `body.items`，並以 JSON 字串傳送每筆 `product` 與 `quantity`。
- 盤點清單已可依區域、類別與商品搜尋篩選，並在頁面上逐筆輸入實際盤點數量。
- 實際盤點數量預設為 `0`，送出後會呼叫 `auditInventory`。
- 盤點工具列與清單版面已完成桌面版排版調整。
- 盤點模式與庫存模式的工具列已分離，各自套用獨立的欄寬設定。
- 已移除庫存模式與盤點模式的重新整理按鈕。

## Phase 6 實作細節（已完成）

### 已完成前端流程

- 購買清單已接上 `getShoppingList`，可依目前區域讀取低於安全庫存的列管商品。
- 工具列已提供類別篩選、商品搜尋與待補貨數量顯示。
- 清單改為兩欄式卡片排列，並支援顯示目前庫存／安全庫存與其他區域庫存。
- 其他區域庫存已改為可讀格式，不會再顯示 `[object Object]`。

### 驗證結果

- `getShoppingList` 已可直接解析後端回傳的中文欄位資料：`區域`、`商品`、`類別`、`庫存`、`其他區域`、`安全存量`、`單位`。
- 購買清單頁已可在未登入狀態下開啟。
- 購買清單工具列與清單卡片版面已完成桌面版調整。

## Phase 7 實作細節（已完成）

### 已完成前端流程

- 購買清單已加入前端勾選狀態，可標記已購買項目。
- 勾選狀態僅保存在目前瀏覽器分頁的前端 state，不會寫回 Google Sheet，也不會跨裝置同步。
- 已勾選項目會以較淡的樣式呈現，方便區分完成狀態。

### 驗證結果

- 勾選／取消勾選會即時反映在畫面上。
- 已勾選卡片會呈現半透明與較淡色調的完成狀態。
- 購買清單右側不再重複顯示多餘的項目數，只保留工具列數量。

## Phase 8 實作細節（已完成）

- 安全庫存設定頁已接上 `getSafetyStock` 與 `updateSafetyStock`。
- 工具列提供類別、狀態、文字篩選與單一儲存按鈕。
- 可批次儲存所有已修改品項；送出資料為 JSON 字串化的 `items` 陣列，包含 `product`、`safetyStock`、`managementStatus`。
- 管理狀態支援列管、暫停、封存與空值；暫停、封存卡片分別以黃色、紅色顯示。
- 已修改卡片以外框標示，安全庫存空白時送出空字串。

## Phase 9 實作細節（已完成）

- 設定頁已加入主題色下拉選單與儲存按鈕。
- 主題色設定會保存於瀏覽器 `localStorage`，並套用至所有頁面。
- 已提供奶油暖陽、海洋湛藍、冷靜灰色、午夜藍與深闇黑主題。
- 深色模式已調整搜尋框、輸入框與安全庫存警示顏色。
- 已移除粉藕色與科技綠主題。

## Phase 10 實作細節（已完成）

- 登入頁、首頁、庫存模式、盤點模式、購買清單與設定頁均拆分為獨立 HTML。
- 各頁透過各自的 JavaScript 模組初始化功能，共用樣式集中於 `styles.css`。
- 頁面導覽改為直接連結各獨立 HTML，登入保護與公開購買清單流程維持正常。

## Phase 11 實作細節（已完成）

### 手機版介面

- 小於 700px 時套用響應式版型，取消桌面版固定最小寬度，並調整頁面間距、卡片、工具列、表單與清單排列。
- 手機版新增固定於畫面下方的五格導覽：庫存、盤點、首頁、購物清單、設定。
- 手機版設定頁分為選擇主題色、安全庫存管理與登出三個入口。
- 盤點頁、購物清單與安全庫存頁新增右下角回到頂端按鈕，並避開底部導覽列。
- 桌面版維持原本上方導覽與設定頁版型；上方導覽項目統一補上 Material Icons。

### 驗證方式

- 以手機寬度開啟首頁、庫存、盤點、購物清單、設定與安全庫存頁，確認底部五格導覽可切換且目前頁面有選取狀態。
- 在盤點、購物清單與安全庫存頁向下捲動後點擊右下角按鈕，確認頁面回到頂端。
- 以桌面寬度開啟設定頁，確認只顯示主題色設定；確認上方首頁、庫存、盤點、購買清單均有 icon。



## Phase 12 需要改善地方
1. ✅ 全部頁面的搜尋不能用
2. ✅ 字體完全不統一
3. ✅ 進入頁面先有全屏 loading 動畫
4. ✅ 庫存頁，減少的數量不可高於目前庫存，要顯示錯誤


## 工作紀錄

| 日期 | 進度摘要 | 下一步 |
| --- | --- | --- |
| 尚未開始實作 | 已完成需求釐清、wireframe 理解與 phase 規劃 | 從 Phase 0 開始 |
| 2026-08-23 | 完成 Phase 0：建立純電腦版共用版型、頂部水平導覽、區域選擇暫存、Apps Script API client 與統一錯誤提示。共用圖示已改用 Google Material Icons。手機版樣式與操作不在本階段製作，留待 Phase 11。 | Phase 1：實作 PIN 登入頁與 token 保存。 |
| 2026-08-23 | 完成 Phase 1：建立 PIN 登入、session token／使用者名稱保存、受保護頁面導向、公開購買清單、登出與登入錯誤處理。 | Phase 2：首頁與區域切換。 |
| 2026-08-23 | 完成 Phase 2：首頁改為不重複功能入口的儀表板，顯示登入者與目前區域；台北／台中選擇會保存於瀏覽器。 | Phase 3：庫存查詢與篩選。 |
| 2026-08-23 | 完成 Phase 3：庫存模式改為單一商品快速異動介面，支援區域、搜尋、類別與商品定位，並對接 `getInventory` 的中文欄位資料。 | Phase 4：庫存增加與減少。 |
| 2026-08-23 | 今日完成 Phase 0–3：桌面版共用介面、PIN 登入、首頁儀表板與庫存商品定位。庫存增加／減少按鈕已完成介面，尚未接上異動 API。 | 下次從 Phase 4 開始：串接 `updateInventory`、更新庫存並寫入 `History`。 |
| 2026-08-24 | 開始 Phase 4：已接上庫存增加／減少按鈕、異動數量驗證、送出鎖定、成功／失敗 Toast，以及異動後重新讀取庫存。 | 以實際 Apps Script 驗證 `updateInventory` 欄位格式與 `History` 寫入，確認後再將 Phase 4 標示為已完成。 |
| 2026-08-24 | 完成 Phase 4：庫存增加／減少已正式串接 `updateInventory`，並完成異動數量重設、超量減少阻擋、成功提示與重新讀取庫存。 | Phase 5：盤點模式。 |
| 2026-08-24 | 開始 Phase 5：盤點模式已接上區域／類別篩選、逐筆盤點輸入與 `auditInventory` 送出流程。 | 以實際 Apps Script 驗證 `auditInventory` 的 payload 與 `History` 寫入格式，確認後再將 Phase 5 標示為已完成。 |
| 2026-08-24 | 完成 Phase 5：盤點模式已完成區域／類別／商品篩選、逐筆實際盤點輸入、`auditInventory` 送出與桌面版版面調整。 | Phase 6：購買清單。 |
| 2026-08-24 | 今日完成盤點與庫存兩個工具列的版面拆分與比例調整，並移除兩頁的重新整理按鈕。盤點模式仍維持可搜尋、可逐筆輸入並可送出盤點。 | 明天從 Phase 6：購買清單開始。 |
| 2026-08-29 | 完成 Phase 6 與 Phase 7：購買清單已可依區域顯示低於安全庫存的列管商品，並加入前端勾選已購買狀態；購買清單頁也完成工具列、兩欄卡片與半透明完成樣式調整。 | Phase 8：安全庫存設定。 |
| 2026-09-09 | 完成 Phase 8：安全庫存設定支援篩選、批次儲存與管理狀態維護。 | Phase 9：主題色設定。 |
| 2026-09-11 | 完成 Phase 9：新增主題色設定、跨頁套用、深色模式輸入框與安全庫存警示色調整，並移除粉藕色與科技綠。 | Phase 10：獨立 HTML 頁面。 |
| 2026-09-10 | 完成 Phase 10：登入、首頁、庫存、盤點、購買清單與設定頁均已拆分為獨立 HTML，並保留各頁 JavaScript 模組與共用樣式。 | Phase 11：手機版介面。 |
| 2026-09-11 | 完成 Phase 11：完成手機響應式版型、底部五格導覽、手機設定三項入口、三個頁面的回到頂端按鈕，並保留桌面版設定頁與導覽版型。 | Phase 12：檢查 Bug。 |
| 2026-09-11 | 開始 Phase 13：盤點頁已接收後端回傳的「購買途徑」，有值時顯示，空值時隱藏。 | 完成庫存頁與購買清單的「購買途徑」顯示。 |
| 2026-09-11 | 取消盤點頁「購買途徑」欄位顯示，回復原盤點頁欄位。 | 重新確認 Phase 13 需求。 |
| 2026-09-11 | Phase 13 改為處理購買清單的「其他區域」顯示：有資料時改為卡片右上角標籤，無資料時隱藏。 | 驗證桌面與手機版標籤位置。 |
| 2026-09-11 | 完成 Phase 13：購買清單已完成其他區域標籤、購買途徑顯示、桌面與手機版排版調整。 | Phase 14：整合測試。 |
